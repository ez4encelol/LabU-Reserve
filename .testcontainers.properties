# Testcontainers Docker client configuration
# Used when running from Git Bash (set DOCKER_HOST=tcp://localhost:2375 in shell)
docker.client.strategy=org.testcontainers.dockerclient.NpipeSocketClientProviderStrategy
