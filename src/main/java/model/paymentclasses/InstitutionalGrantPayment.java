package model.paymentclasses;

public class InstitutionalGrantPayment extends PaymentStrategy{

	@Override
	public void makePayment(int amount) {
		// This should be payment processing with the Institution, 
		// but that is outside of the scope of the project, 
		// so just leave here for reference
	}

}
