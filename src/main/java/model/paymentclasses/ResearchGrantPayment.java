package model.paymentclasses;

public class ResearchGrantPayment extends PaymentStrategy{

	@Override
	public void makePayment(int amount) {
		// This should be payment processing with the government, 
		// but that is outside of the scope of the project, 
		// so just simulate it
		
	}

}
